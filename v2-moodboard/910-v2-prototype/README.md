# 910studio v2 — scroll reel prototype

The standalone version of the S1→S4 reel, dev controls included. This is the
sandbox for tuning; the production port lives in `app/` + `lib/reel/engine.ts`.

Source of truth is `../s1-hero.html` — `index.html` here is that file with the
star asset repointed at the deploy root.

    cd v2-moodboard/910-v2-prototype
    npx vercel --prod
